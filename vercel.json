{
    "version": 2,
    "name": "EasePal",
    "root": "frontend/",
    "builds": [
      {
        "use": "@vercel/static-build",
        "config": { "distDir": "dist" }
      }
    ],
    "routes": [
      { "src": "/(.*)", "dest": "/dist/$1" }
    ]
  }